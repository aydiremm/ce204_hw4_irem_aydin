package ce204_hw4_lib.payment;


/**
 * @name PaymentStrategy
 * @brief The PaymentStrategy interface represents a payment strategy.
 * 
 * It provides a common interface for implementing different payment strategies.
 */
public interface PaymentStrategy {
	
	 /**
     * @brief Makes a payment for the specified amount.
     * 
     * This method is implemented by concrete payment strategies to perform the payment process.
     * 
     * @param amount the amount to be paid
     * 
     * @retval None
     */
    void pay(double amount);
}

